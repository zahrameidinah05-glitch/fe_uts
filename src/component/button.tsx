// interface buttonProps {
//   title: string; //title akan berubah beruabha tergantung dari fe nya
//   variant: string; 
// }

// // untuk membuat componenet di react

// const Button: React.FC<buttonProps> = ({ title, variant }) => {
//     const primary= "bg-brand-primary text-white py-2 px-4 rounded";
//     const secondary = "bg-brand-secondary text-white py-2 px-4 rounded";
    
//   return <button className={variant === "primary" ? primary : secondary}> {title}</button>;
// };

// export default Button;